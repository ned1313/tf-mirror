apiVersion: v1
kind: Service
metadata:
  name: {{ include "terraform-mirror.fullname" . }}
  labels:
    {{- include "terraform-mirror.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "terraform-mirror.selectorLabels" . | nindent 4 }}
